# Base URL of the backend API. No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional API key sent as X-API-Key. Leave blank when the backend has auth off.
# NOTE: NEXT_PUBLIC_* values are embedded in the client bundle and are not secret.
NEXT_PUBLIC_API_KEY=
